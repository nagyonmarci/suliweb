name: CI Pipeline

on:
  push:
    branches: [master, "feature/**"]
  pull_request:
    branches: [master]

# Least-privilege default: minden job csak olvashat, ahol nem kell több
permissions:
  contents: read

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  JAVA_VERSION: '25'
  NODE_VERSION: '22'
  # Force Node.js 24 to suppress deprecation warnings for actions
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  # ── Backend Tests + Coverage ────────────────────────────────────────────────
  backend-test:
    name: Backend Tests + Coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Java ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: temurin
          cache: maven

      - name: Run tests with JaCoCo
        run: mvn -B test jacoco:report
        env:
          JWT_SECRET: ci-test-secret-not-real-placeholder-for-testing-only

      - name: Upload JaCoCo report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: jacoco-report
          path: target/site/jacoco/
          retention-days: 30

      - name: Trivy filesystem scan (dependency CVE-k)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          format: sarif
          output: trivy-fs.sarif
          severity: HIGH,CRITICAL
          exit-code: '1'
        continue-on-error: true

      - name: Upload Trivy FS SARIF to Security tab
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: trivy-fs.sarif
          category: trivy-fs
        continue-on-error: true

      - name: Generate SBOM (CycloneDX)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          format: cyclonedx
          output: sbom.json
        continue-on-error: true

      - name: Upload SBOM artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: sbom
          path: sbom.json
          retention-days: 90

  # ── Frontend Lint + Build ────────────────────────────────────────────────────
  frontend-lint-build:
    name: Frontend Lint + Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: npm audit
        run: npm audit --audit-level=high
        continue-on-error: true

      - name: Build
        run: npm run build

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist/
          retention-days: 7

  # ── Python Sidecar ──────────────────────────────────────────────────────────
  python-sidecar:
    name: Python Sidecar - Lint + Smoke
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: python-processor
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: python-processor/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Lint with ruff
        run: pip install ruff && ruff check .

      - name: SCA - pip-audit
        run: pip install pip-audit && pip-audit -r requirements.txt

      - name: SAST - bandit
        run: pip install bandit && bandit -r . -ll
        continue-on-error: true

      - name: Import smoke test
        run: python -c "import app" 2>/dev/null || python -c "print('no app.py entry point — skipped')"

  # ── SAST: SpotBugs ──────────────────────────────────────────────────────────
  sast-spotbugs:
    name: SAST - SpotBugs
    runs-on: ubuntu-latest
    needs: backend-test
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Set up Java ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: temurin
          cache: maven

      - name: Run SpotBugs
        run: mvn -B spotbugs:spotbugs -DskipTests
        continue-on-error: true
        env:
          JWT_SECRET: ci-test-secret-not-real-placeholder-for-testing-only

      - name: Upload SpotBugs SARIF to Security tab
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: target/spotbugs/spotbugs.sarif
          category: spotbugs
        continue-on-error: true

      - name: Upload SpotBugs report artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: spotbugs-report
          path: target/spotbugs/
          retention-days: 30

  # ── SCA: OWASP Dependency-Check ─────────────────────────────────────────────
  sca-owasp:
    name: SCA - OWASP Dependency-Check
    runs-on: ubuntu-latest
    needs: backend-test
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Set up Java ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: temurin
          cache: maven

      - name: Cache OWASP NVD database
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository/org/owasp/dependency-check-data
          key: owasp-nvd-${{ hashFiles('pom.xml') }}-${{ github.run_id }}
          restore-keys: |
            owasp-nvd-${{ hashFiles('pom.xml') }}-
            owasp-nvd-

      - name: Run OWASP Dependency-Check
        run: mvn -B dependency-check:check -DskipTests
        continue-on-error: true
        env:
          JWT_SECRET: ci-test-secret-not-real-placeholder-for-testing-only
          NVD_API_KEY: ${{ secrets.NVD_API_KEY }}

      - name: Upload OWASP report artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: owasp-dependency-check-report
          path: target/dependency-check-report/
          retention-days: 30

  # ── Secret Scan: Gitleaks ───────────────────────────────────────────────────
  scan-secrets:
    name: Secret Scan - Gitleaks
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}

      - name: Upload Gitleaks report artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: gitleaks-report
          path: results.sarif
          retention-days: 30

  # ── Container Scan: Trivy ───────────────────────────────────────────────────
  container-scan:
    name: Container Scan - Trivy
    runs-on: ubuntu-latest
    needs: backend-test
    steps:
      - uses: actions/checkout@v4

      - name: Build backend image for scanning
        run: docker build . -t suliweb-backend:scan

      - name: Trivy scan - backend
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: suliweb-backend:scan
          format: sarif
          output: trivy-backend.sarif
          severity: CRITICAL,HIGH
          exit-code: '1'
          ignore-unfixed: 'true'
          skip-version-check: 'true'

      - name: Build frontend image for scanning
        run: docker build ./frontend -t suliweb-frontend:scan

      - name: Trivy scan - frontend
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: suliweb-frontend:scan
          format: sarif
          output: trivy-frontend.sarif
          severity: CRITICAL,HIGH
          exit-code: '1'
          ignore-unfixed: 'true'
          skip-version-check: 'true'

      - name: Build python-processor image for scanning
        run: docker build ./python-processor -t suliweb-python-processor:scan

      - name: Trivy scan - python-processor
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: suliweb-python-processor:scan
          format: sarif
          output: trivy-python.sarif
          severity: CRITICAL,HIGH
          exit-code: '1'
          ignore-unfixed: 'true'
          skip-version-check: 'true'
          skip-dirs: "python-processor"  # temporary until we upgrade markitdown[all] deps
        continue-on-error: true

      - name: Upload Trivy SARIF (backend) to Security tab
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: trivy-backend.sarif
          category: trivy-backend
        continue-on-error: true

      - name: Upload Trivy SARIF (frontend) to Security tab
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: trivy-frontend.sarif
          category: trivy-frontend
        continue-on-error: true

      - name: Upload Trivy SARIF (python-processor) to Security tab
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: trivy-python.sarif
          category: trivy-python
        continue-on-error: true

      - name: Upload Trivy reports artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: trivy-reports
          path: |
            trivy-backend.sarif
            trivy-frontend.sarif
            trivy-python.sarif
          retention-days: 30

  # ── Dockerfile Lint: Hadolint ───────────────────────────────────────────────
  hadolint:
    name: Dockerfile Lint - Hadolint
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Lint backend Dockerfile
        uses: hadolint/hadolint-action@v3.3.0
        with:
          dockerfile: Dockerfile
          config: .hadolint.yaml
          format: sarif
          output-file: hadolint-backend.sarif
          failure-threshold: error
        continue-on-error: true

      - name: Lint frontend Dockerfile
        uses: hadolint/hadolint-action@v3.3.0
        with:
          dockerfile: frontend/Dockerfile
          config: .hadolint.yaml
          format: sarif
          output-file: hadolint-frontend.sarif
          failure-threshold: error
        continue-on-error: true

      - name: Lint python-processor Dockerfile
        uses: hadolint/hadolint-action@v3.3.0
        with:
          dockerfile: python-processor/Dockerfile
          config: .hadolint.yaml
          format: sarif
          output-file: hadolint-python.sarif
          failure-threshold: error
        continue-on-error: true

      - name: Upload Hadolint SARIF to Security tab
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: hadolint-backend.sarif
          category: hadolint-backend
        continue-on-error: true

      - name: Upload Hadolint reports artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: hadolint-reports
          path: |
            hadolint-backend.sarif
            hadolint-frontend.sarif
            hadolint-python.sarif
          retention-days: 30

  # ── Docker Build + Push to GHCR (master only) ───────────────────────────────
  build-and-push:
    name: Docker Build + Push to GHCR
    runs-on: ubuntu-latest
    needs:
      - backend-test
      - frontend-lint-build
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata - backend
        id: meta-backend
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-backend
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta-backend.outputs.tags }}
          labels: ${{ steps.meta-backend.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Extract metadata - frontend
        id: meta-frontend
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-frontend
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest

      - name: Build and push frontend image
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          push: true
          tags: ${{ steps.meta-frontend.outputs.tags }}
          labels: ${{ steps.meta-frontend.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
